name: "Vito"
version: "2.2.0"
slug: "vito"
description: "Vito web interface inside Home Assistant"
arch:
  - amd64
  - aarch64
  - armv7
startup: application
boot: auto
init: false
ingress: false
panel_icon: mdi:server
ports:
  8000/tcp: 8089
ports_description:
  8000/tcp: "Vito web interface (Laravel Sail)"
privileged:
  - SYS_ADMIN
  - NET_ADMIN
docker_api: true
full_access: true
map:
  - config:rw
  - share:rw
  - ssl:ro
  - media:rw
  - addon_config:rw
options:
  name: "vito"
  email: "admin@example.com"
  password: "password"
  app_url: "http://homeassistant.local:8123"
schema:
  name: str
  email: str
  password: str
  app_url: str
